[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "app-excel-import"
version = "0.1.0"
description = "MFDApps Excel import service for loading workbook content into a database"
requires-python = ">=3.11"
