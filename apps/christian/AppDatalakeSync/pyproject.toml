[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "app-datalake-sync"
version = "0.1.0"
description = "MFDApps Data Lake <-> Fabric SQL table synchronization service"
requires-python = ">=3.11"
